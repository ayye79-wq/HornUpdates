#!/bin/bash
set -e

# Horn Updates is a static HTML site — no build step, no package installs.
# This script just confirms the environment is healthy after a merge.

echo "Post-merge setup: static site, nothing to build."
echo "Done."
