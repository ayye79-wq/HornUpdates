# Horn Updates

## Project Overview
HornUpdates is an automated news aggregation and summarization platform focused on the Horn of Africa (Ethiopia, Somalia, Sudan, South Sudan, Eritrea, Djibouti, and Kenya). It scrapes news from RSS feeds and Telegram channels, filters them for regional relevance, and presents them in a clean web-based interface.

## Architecture
- **Frontend**: Static HTML/CSS/JavaScript (no framework). PWA-enabled with service worker and manifest.
- **Data**: Pre-generated `articles.json` file fetched by the frontend at runtime.
- **Sub-site**: `/ethio/` directory contains the Ethio Pulse sub-app focused on Ethiopian news.
- **Backend scripts**: Python scripts for data ingestion (not run as a persistent server).

## Key Files
- `index.html` — Main site entry point
- `style.css`, `script.js`, `app.js` — Main site styling and logic
- `articles.json` — News data (generated by Python scripts)
- `ethio/` — Ethio Pulse sub-site with its own HTML/CSS/JS and `ethio_articles.json`
- `update_articles.py` — RSS feed scraper (run manually or on schedule)
- `update_pulse.py` — Telegram/RSS scraper for Ethio Pulse
- `generate_rss.py` — Generates `rss.xml`
- `reader.html` — Full article reader page

## Development Server
- Served via Python's built-in HTTP server on port 5000
- Command: `python3 -m http.server 5000 --bind 0.0.0.0`

## Deployment
- Deployed as a **static site** (publicDir: `.`)
- No build step required
