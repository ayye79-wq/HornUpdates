#!/usr/bin/env python3
"""Retired generator for the former external-link news feed.

`feed.xml` is now a small, editor-selected feed of Horn Updates pages. This
compatibility entry point is intentionally non-writing so a manual command cannot
replace it with an unreviewed aggregation feed.
"""

raise SystemExit(
    "Automatic RSS generation is retired. Update feed.xml only with reviewed "
    "Horn Updates entries."
)
